// Type Conversion in Javascript

// Type conversion = change the datatype of a value to another(strings, numbers, booleans)


// EXAMPLE 1

// let age = window.prompt("How old are you?");
// age = Number(age);                                   ((Run this example with and without this line))
// age+=1;

// console.log(age, typeof age);



// -------------- EXAMPLE 2 --------------

let x = "pizza";    // try with other like   = ""    = "0"  = 0   just with declaration
let y = "pizza";    // try with other like   = ""    = "0"  = 0   just with declaration
let z = "pizza";    // try with other like   = ""    = "0"  = 0   just with declaration

x = Number(x);
y = String(y);
z = Boolean(z);

console.log(x, typeof x);
console.log(y, typeof y);
console.log(z, typeof z);